feat: live dashboard v2, ATR quant model, security hardening

Live Dashboard:
- Remove pre-loaded default tickers; start empty, max 8 tickers
- 2-row x 4-col card grid layout with colorful signal-driven backgrounds
- Color-coded fields: Spread, ATR, Volume, Best for Long/Short
- Fix duplicate heatmap cards (removed heatmap strip)
- Fix ATR calculation using wrong column (Volume instead of Close)
- Replace CSS grid with table layout to fix Streamlit HTML rendering

ATR Analysis:
- Add Quant Probability Model: 4 ATR bands (0.5/1.0/1.5/2.0x) with hit probabilities
- Add Expected Next Day Levels (Expected, Max Intraday, Extreme High/Low)
- Add Volatility Compression Detection (3+ consecutive falling ATR sessions)
- Add Target Hit Probability scoring per trade setup
- Add Trade Quality Score 0-100 (trend, R:R, probability, PCR sentiment)
- Plot ATR probability bands on candlestick chart as dashed/dotted lines

Security / Access Control:
- Bind server to LAN IP only (not 0.0.0.0) to block external access
- Add floating hamburger button to toggle sidebar visibility
- Sidebar starts collapsed by default
